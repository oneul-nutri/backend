[pytest]
asyncio_mode = auto
# 테스트 클래스의 @mark.asyncio(loop_scope="session") 과 반드시 일치해야 한다.
# 불일치 시 fixtures는 session loop, tests는 function loop에서 실행되어
# asyncmy가 "Future attached to a different loop" RuntimeError를 발생시킨다.
asyncio_default_fixture_loop_scope = session
